{
  "name": "pinterest-scraper-api",
  "version": "1.0.0",
  "description": "A serverless API to search for Pinterest images without an API key",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "node build.js",
    "start": "wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pinterest",
    "api",
    "scraper",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.0"
  },
  "dependencies": {}
}
